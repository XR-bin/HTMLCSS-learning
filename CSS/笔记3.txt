1. text-shadow

    文字的阴影
        text-shadow: x  y  blur   color
                     位置  模糊度  颜色

    注：阴影的默认颜色是跟文字样色相同
    注：通过逗号的方式进行分割，可以设置多阴影



2. box-shadow

    盒子阴影
        box-shadow: x y   blur  spread     color  inset
                    位置  模糊度 阴影的大小  颜色   内阴影(只能选内)
                
    注：盒子阴影的默认样色是黑色。
    注：默认就是外阴影，如果设置outset不起作用，可选的值只有inset表示内阴影。
    注：通过逗号的方式进行分割，可以设置多阴影



3. box-reflect

    above上 below下 left左 right右
	距离
	遮罩 | 渐变 

    渐变：只是针对透明度的渐变，不能支持颜色的渐变。
    
    渐变函数linear-gradient()

    注：box-reflect目前还没有标准化，所以需要添加浏览器前缀。(有可能后来就不能用了)


4. 图片模糊效果

    filter 属性定义了元素(通常是<img>)的可视效果(例如：模糊与饱和度)。

    blur(像素值);       模糊效果   例如： filter: blur(20px)
    grayscale(百分比);  饱和度     例如： filter: grayscale(50%)



5. calc计算
    calc() 函数用于动态计算长度值。

    【注意】
        1) 需要注意的是，运算符前后都需要保留一个空格，例如：width: calc(100% - 10px)；
        2) 任何长度值都可以使用calc()函数进行计算；
        3) calc()函数支持 "+", "-", "*", "/" 运算；
        4) calc()函数使用标准的数学运算优先级规则；




6. 分栏布局

    分栏布局有点类似于报纸上的文字布局，都是一列一列的，主要由下来css样式实现。

    column-count ： 分栏的个数
    column-width ： 分栏的宽度
    column-gap   ： 分栏的间距
    column-rule  ： 分栏的边线
    column-span  ： 合并分栏

    注：column-width和column-count不要一起去设置。



7. 伪类和伪元素

    在CSS2.1中对伪类和伪元素的区别比较模糊。
    CSS3中对这两个概念做了相对较清晰地解释，
    并且在语法上也做了很明显的区分。

    CSS3中规定伪类由一个冒号开始，然后为伪类的名称。
    伪元素由两个冒泡开始，然后为伪元素的名称。

    1) 伪类
        伪类本质上是为了弥补常规CSS选择器的不足，
        以便获取到更多信息。通常表示获取不存在与
        DOM树中的信息，或获取不能被常规CSS选择器
        获取的信息。

        例如 :hover  移入触发

    2) 伪元素
        伪元素本质上是创建了一个有内容的虚拟容器。
        这个容器不包含任何DOM元素，但是可以包含内
        容。另外，开发者还可以为伪元素定制样式。

        例如 ::after   在某个元素内最后的子元素后创建一个容器

    【总结】伪类针对的是父元素，伪元素针对的是子元素



8. CSS  Hack分类    

    Hack分类主要是针对IE低版本浏览器

    1) CSS属性前缀法
        前缀标识       兼容浏览器
          _              IE6
         +/*           IE6、IE7
          \9          IE6/7/8/9
          \0          IE8/9/10/11

        例如：.类名{ _background:red; }

    2) 选择器前缀法
        前缀标识       兼容浏览器
         *html           IE6
         *+html          IE7
         :root          IE9及以上

        例如： *html .类名{ }

    3) IE条件注释法
                前缀标识               兼容浏览器
        <!--[if IE]>...<![endif]>         IE
        <!--[if IE 7]>...<![endif]>       IE7
        <!--[if gte IE 7]>...<![endif]>   IE7一下
        <!--[if ! IE 7]>...<![endif]>     非IE7

        IE10以上已经不支持注释法



11. 布局扩展
    1) 等高布局 
        利用margin-bottom负值与padding-bottom配合实现

    2) 三列布局，左右固定，中间自适应，以下方式可实现
        1. BFC方式
        2. 定位
        3. 浮动(典型例子：双飞翼布局、圣杯布局)  都是利用margin负值实现的
        4. flex弹性
